name: Setup Python
description: MarkText wrapper for actions/setup-python@v5.6.0

inputs:
  python-version:
    description: Python version to install
    required: true

runs:
  using: composite
  steps:
    - uses: actions/setup-python@v5.6.0
      with:
        python-version: ${{ inputs.python-version }}
